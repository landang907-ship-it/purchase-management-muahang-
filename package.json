{
  "name": "root-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd purchase-management && npm install && npm run build && node -e \"const fs=require('fs'); fs.rmSync('../dist', { recursive: true, force: true }); fs.renameSync('dist', '../dist');\""
  }
}
